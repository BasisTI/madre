export class UF {
    id: number;
    unidadeFederativa: string;
    sigla: string;

    constructor(id: number, unidadeFederativa: string, sigla: string) {
        this.id = id;
        this.unidadeFederativa = unidadeFederativa;
        this.sigla = sigla;
    }
}
